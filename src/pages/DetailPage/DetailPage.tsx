import styles from "./DetailPage.module.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);
export default function DetailPage() {
  return (
    <div>
      <span>Detail Page 1223</span>
    </div>
  );
}
